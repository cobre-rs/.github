# Org-wide issue defaults, inherited by every cobre-rs repo without its own.
# Kept minimal. Re-add a `contact_links:` entry here (or override per-repo) once
# GitHub Discussions is enabled on a repo to point people to.
blank_issues_enabled: true
